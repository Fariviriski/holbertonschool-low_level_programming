#include "main.h"
/**
 *
 *
 *
 */

void rev_string(char *s)
{
	int i, len, temp;

	lenght = _strlen(s) -1;
	for (i = 0; i < _strlen(s)/2; i++0
			{
			temp = s[i];
			s[i] = s[len];
			s[len--]=temp;
			}
			_putchar(s)
			}

