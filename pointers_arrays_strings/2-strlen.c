#include "main.h"
/**
 * _strlen - prints string lenght
 * @s: string of characters to be deferenced
 * Return: lenght of pointed string
 */

int _strlen(char *s)
{
	int lenght;

	lenght = _strlen(s);

		return (lenght);
}
