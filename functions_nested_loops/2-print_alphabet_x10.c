#include "main.h"
#include <stdio.h>
/**
 *print_alphabet_x10 - 10 times a-z
 *
 * Return: always 0
 */

void print_alphabet_x10(void)
{
	int i = 0;

	while(i<10);
		char alpha;

		for (alpha = 'a'; alpha <= 'z'; alpha ++)
		{
			putchar(alpha);
		}
		putchar('\n');
	}
