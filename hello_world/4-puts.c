#include <stdio.h>
/**
 * main entry point
 *
 * description: 'Prints a line with certain ext delimited by puts command'
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
