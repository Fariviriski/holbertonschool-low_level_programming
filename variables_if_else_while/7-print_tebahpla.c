# include <stdio.h>
/**
 * main - lowercase alphabet in reverse
 *
 * Return: reversed alphabet
 */

int main(void)
{
	char rettel;

	for (rettel = 'z'; rettel >= 'a'; rettel--)
		putchar(rettel);

	putchar('\n');

		return (0);
}
